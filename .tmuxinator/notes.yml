# ~/.tmuxinator/do.yml

name: notes
root: ~/Drive/notes

# Project hooks
# Runs on project start, always
on_project_start: task sync
# Run on project exit ( detaching from tmux session )
on_project_exit: task sync

windows:
  - index:
      layout: main-vertical
      panes:
        - nvim index.md
